buildscript {
    repositories {
        mavenCentral()
    }
    dependencies {
        classpath("org.springframework.boot:spring-boot-gradle-plugin:1.5.22.RELEASE")
    }
}

apply plugin: 'java'
apply plugin: 'org.springframework.boot'

group = 'org.gamer'
version = '1.0-SNAPSHOT'

jar {
    baseName = 'video-service'
    version =  '0.1.0'
}


repositories {
    mavenCentral()
}

configurations {
    compile.exclude module: "spring-boot-starter-tomcat"
}

sourceCompatibility = 1.8
targetCompatibility = 1.8

dependencies {
    testCompile 'junit:junit:4.13.2'
    testCompile 'org.assertj:assertj-core:3.19.0'
    compile 'com.google.apis:google-api-services-youtube:v3-rev222-1.25.0'
    compile 'org.springframework.boot:spring-boot-starter-web:1.5.22.RELEASE'
    compile 'org.springframework.boot:spring-boot-starter-undertow:1.5.22.RELEASE'
    compile 'org.springframework.boot:spring-boot-starter-actuator:1.5.22.RELEASE'
    compile 'org.springframework.boot:spring-boot-starter-data-redis:1.5.22.RELEASE'
    compile 'com.orange.redis-embedded:embedded-redis:0.6'
}

tasks.withType(JavaCompile) {
     options.encoding = 'UTF-8'
}
